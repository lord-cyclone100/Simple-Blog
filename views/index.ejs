<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="./css/style.css">
</head>
<body>
    <div class="main">
        <div class="box left">
            <h1><span>CRUD</span> Blogger</h1>
            <p>Create, Read, Update and Delete Blogs</p>
            <a href="/new"><button><strong>Create Blog</strong></button></a>
        </div>
        <div class="right">
            <div class="content">
                <h1 class="head">Your Posts</h1>
            </div>

            <% blogs.forEach((item)=>{ %> 
                <div class="pst">
                    <h2><%= item.title %></h2>
                    <p><%= item.content %></p>
                    <div>
                        <a href="/edit/<%= item.id %>"><button>Edit</button></a>
                        <a href="/delete/<%= item.id %>"><button>Delete</button></a>
                    </div>
                </div>
            <% }) %>

        </div>
        <%- include('partials/footer') %>
    </div>
</body>
</html>